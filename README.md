# Wordlist Arcade: paste a vocab list, get six playable classroom games, unlimited and account-free

Live: https://wordlist-arcade.sociobot.in — built by the Param Factory (`static-web`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
